<table class = "table_article">
	<tr>
		<td>
			<h3><%= @article.title %></h3> <h5><i>by <%= @user.find(@article.user_id).username %></i></h5>
		</td>
	</tr>
	<tr>
		<td>
			<%= @article.text %>
		</td>
	</tr>
</table>

COMMENTS:<br />

<%= render @article.comments %>
<%= render 'comments/form' %>
<%= link_to 'Back', articles_path %> | <% if @article.user_id == current_user.id %> <%= link_to 'Edit', edit_article_path %> <% end %>	